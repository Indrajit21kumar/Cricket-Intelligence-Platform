"""Knowledge-graph domain — the ontology, rule schema, lifecycle, and serving.

Pure logic with no I/O: the ontology vocabulary (Book 4 Ch. 5), the canonical
rule schema + validation (Book 4 Ch. 6), the authoring lifecycle, and the
fact-pattern matcher. The service layer wraps these with the store.
"""
