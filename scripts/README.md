# scripts/

Utility scripts. The main one — `scaffold_service.py` — is written during M01
Step 7 and copies `services/reference-service/` into a new service directory
with correct package renames and pyproject metadata.
